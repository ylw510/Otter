# Extension icons

- **`otter-mascot.svg`** — vector source (anime-style otter mascot for Otter).
- **`icon16.png` / `icon48.png` / `icon128.png`** — generated for `manifest.json` (run `npm run icons` from `extension/`).

**License:** Original artwork for this repository (see project `LICENSE`). Regenerate PNGs after editing the SVG.
