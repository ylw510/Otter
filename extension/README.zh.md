# Otter — Chrome 扩展

[English](README.en.md)

该目录是 Otter 面向用户的唯一 UI。

## 开发

- `npm install`
- `npm run dev`

在 `chrome://extensions` 打开开发者模式，加载 `extension/dist`。

## 构建

- `npm run build`
- 产物目录：`extension/dist/`
